name: example
description: "Example app demonstrating how to use the flutter_page_object library."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.2.6 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

  flutter_page_object:
    path: ../

  test_common:
    path: test_common

flutter:
  uses-material-design: true
